#include "main.h"
#include <stdio.h>
/**
 * _strlen - prints length of a string
 * @s: parameter
 * Return: length of the string
 */

int _strlen(char *s)
{
	int i;

	strl;
	*s[] = "Software";

	for (i = 0; s[i] != '\0'; i++)
	{
		strl = putchar("%d\n", i);
	}
	return (strl);
}

