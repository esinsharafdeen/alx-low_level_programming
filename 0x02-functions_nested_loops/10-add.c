#include "main.h"

/**
 * add - adda two integers and returns the result
 * @i: prints int i
 * @k: prints int k
 * Return: Always sum
 */

int add(int i, int k)
{
	int sum;

	sum = i + k;

	return (sum);
}
